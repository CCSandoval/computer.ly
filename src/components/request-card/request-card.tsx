import React from "react";

interface RequestCardProps {
  data: {};
}

const RequestCard: React.FC<RequestCardProps> = ({ data }) => {
  return <div>RequestCard</div>;
};

export default RequestCard;
